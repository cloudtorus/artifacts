version: v1
name: Metabase
description: Business Intelligence For Everyone
tags:
  - SaaS
paths:
  - helm/**
  - 'terraform/{{provider}}/**'
  - terraform/helm/**
providers:
  - google
  - digitalocean
dependencies:
  - primitives/cluster
  - ref: primitives/sql
    name: Metabase's Database
    requirements:
      engine: pg
    unique: true
